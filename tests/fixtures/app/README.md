This is a sample app used with the integration tests.

With a configured web server (like this project's Dockerfile), the app can be viewed directly too: `http://localhost/`

This project's `Dockerfile` uses the generic PHP server, see `router.php`.
